#ifndef MODULE_H

#include "atom.h"           // * the module call ATOM class
#include "particle.h"       // * the module call PARTICLE class
#include "polynomial.h"     // * the module call POLYNOMIAL class
#include "matrix.h"         // * the module call MATRIX class

#include "IOhandler.h"      // * calling method for Arr2Poly & Poly2Arr
#include "linalg.h"         // * calling operations on matrix

#endif